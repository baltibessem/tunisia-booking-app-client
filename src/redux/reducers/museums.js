

const initialState = {
  museums: [],
};

const museumsReducer = (state = initialState, action) => {
  const { type, payload } = action;
  switch (type) {
     
      default:
          break;
  }


};
